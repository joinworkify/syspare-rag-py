# RAG API base URL when the viewer runs in another repo or different origin.
# Examples:
#   Same repo (RAG server on same host): leave empty or omit
#   RAG server on Render: https://your-rag-app.onrender.com
#   Local RAG server: http://localhost:8000
VITE_RAG_API_URL=http://localhost:8000

# Copy to .env and fill in real values. Never commit .env.

# ----- Google Cloud (Vertex AI) -----
PROJECT_ID=your-gcp-project-id
LOCATION=us-central1
# Path to service account JSON (optional if using gcloud application-default login)
# GOOGLE_APPLICATION_CREDENTIALS=/path/to/service-account.json

# ----- Local paths (RAG data & cache) -----
PDF_FOLDER=./data/
CACHE_DIR=./cache_ym358a
IMAGE_DIR=./cache_ym358a/images

# ----- AWS S3 (optional; cache & PDFs synced to bucket) -----
# AWS_ACCESS_KEY_ID=
# AWS_SECRET_ACCESS_KEY=
# AWS_DEFAULT_REGION=us-east-2
# S3_BUCKET_NAME=your-bucket-name
# S3_RAG_PREFIX=rag-data
